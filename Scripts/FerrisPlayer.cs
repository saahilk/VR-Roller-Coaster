using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FerrisPlayer : MonoBehaviour
{
    Transform ferris;
    // Start is called before the first frame update
    void Start()
    {
        // ferris =
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
